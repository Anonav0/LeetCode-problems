# 🧩 Problem: <LC338. Counting Bits>

**LeetCode Link:** https://leetcode.com/problems/counting-bits/description/
**Difficulty:** Easy  
**Topics:** Array, Bit Manupilation, Dynamic Programming.  
**Date Solved:** 23rd March 2026

---

## 📜 Problem Statement

Given an integer n, return an array ans of length n + 1 such that for each i (0 <= i <= n), ans[i] is the number of 1's in the binary representation of i.

Example 1:

Input: n = 2
Output: [0,1,1]
Explanation:
0 --> 0
1 --> 1
2 --> 10

Example 2:

Input: n = 5
Output: [0,1,1,2,1,2]
Explanation:
0 --> 0
1 --> 1
2 --> 10
3 --> 11
4 --> 100
5 --> 101

Constraints:

    0 <= n <= 105

Follow up:

    It is very easy to come up with a solution with a runtime of O(n log n). Can you do it in linear time O(n) and possibly in a single pass?
    Can you do it without using any built-in function (i.e., like __builtin_popcount in C++)?

---

## 🔍 Key Observations

---

# 🔹 LC 338 — Counting Bits

### 🧠 Problem

Given `n`, return an array `ans` where:

```text
ans[i] = number of 1's in binary representation of i
```

Example:

```text
n = 5
→ [0,1,1,2,1,2]
```

---

# 🔹 Step 1: Brute Force

For each number:

- Convert to binary
- Count 1s

👉 Time = `O(n log n)` ❌

We want **O(n)**

---

# 🔹 Step 2: Key Insight (THIS IS EVERYTHING)

Look at patterns:

```text
0  -> 0
1  -> 1
2  -> 10 -> 1
3  -> 11 -> 2
4  -> 100 -> 1
5  -> 101 -> 2
6  -> 110 -> 2
7  -> 111 -> 3
```

---

# 🔥 Observation

👉 Every number is related to a **smaller number**

Two powerful approaches:

---

# 🔹 Approach 1: Last Bit Trick

If `i` is even:

```text
i = i/2 << 1 → same bits
```

If `i` is odd:

```text
i = (i-1) + 1 bit
```

👉 So:

```text
dp[i] = dp[i / 2] + (i % 2)
```

---

# 🔹 Approach 2 (Cleaner): Remove Last Set Bit

👉 Key identity:

```text
i & (i - 1)
```

This removes the **last set bit**

---

### 💡 So:

```text
dp[i] = dp[i & (i - 1)] + 1
```

---

# 🔹 Visualization

dp[i] = dp[i & (i-1)] + 1

---

# 🔹 Why This Works

Example:

```text
i = 6 → 110
i & (i-1) = 100 → 4
```

So:

```text
dp[6] = dp[4] + 1 = 1 + 1 = 2
```

---

# 🔹 Code (Best Approach)

```java
class Solution {
    public int[] countBits(int n) {
        int[] dp = new int[n + 1];

        for (int i = 1; i <= n; i++) {
            dp[i] = dp[i & (i - 1)] + 1;
        }

        return dp;
    }
}
```

---

# ⚡ Complexity

- Time: `O(n)`
- Space: `O(n)`

---

# 🔥 Pattern You Should Remember

👉 This is:

> **DP + Bit Manipulation + "reduce to smaller problem"**

---
